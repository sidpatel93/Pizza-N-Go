
<nav class="main-nav">
  <% if(user) { %>
      <% if(user.isadmin === true) { %>
        <ul class="nav-items">
          <li class="logo"><a href="/admin/orders/new">P<i class="fas fa-pizza-slice"></i>zza N Go</a></li>
          <ul class="nav-links">
            <form action="/logout" method="POST" id="logoutForm">
              <li><a ref="#" id="logOut">Logout</a></li>
            </form>
          </ul>
        </ul>
      <% } else { %>
        <ul class="nav-items">
          <li class="logo">
            <a href="/">P<i class="fas fa-pizza-slice"></i>zza N Go</a>
          </li>
          <ul class="nav-links">
            <li>
              <a class="navbarCart" href="/cart">Cart (<%= session.cart ? session.cart.totalQty : " "%>)</a>
            </li>
            <form action="/logout" method="POST" id="logoutForm">
              <li>
                <a ref="#" id="logOut">Logout</a>
              </li>
            </form>
          </ul>
        </ul>
    <% } %>
  <% } else { %>
    <ul class="nav-items">
      <li class="logo">
        <a href="/">P<i class="fas fa-pizza-slice"></i>zza N Go</a>
      </li>
      <ul class="nav-links">
        <li><a href="/login">Login</a></li>
        <li><a href="/register">Register</a></li>
        <li><a class="navbarCart"href="/cart">Cart (<%= session.cart ? session.cart.totalQty : " "%>)</a></li>
      </ul>
    </ul>
  <% } %>
</nav>